backend:
  name: github
  repo: ictintegratedhub/ACE-ICT-INTEGRATED-HUB
  branch: main
  # For PAT authentication, you need this:
  auth_type: implicit
  # Don't remove these - they're required even for PAT:
  app_id: missing  # This tells CMS to use PAT mode
  
media_folder: static/images/uploads
public_folder: /static/images/uploads

collections:
  - name: posts
    label: Blog Posts
    folder: content/posts
    create: true
    extension: md
    format: yaml-frontmatter
    fields:
      - name: title
        label: Title
        widget: string
      - name: date
        label: Date
        widget: datetime
      - name: thumbnail
        label: Thumbnail Image
        widget: image
        required: false
      - name: body
        label: Body
        widget: markdown